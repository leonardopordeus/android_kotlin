<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="br.com.ceiot.aula09">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <!-- TODO (9) Adicionar Servico no Manifest -->
        <service
                android:name=".SendDataIntentService"
                android:exported="false">
        </service>

        <provider
                android:name=".AppCeiotContentProvider"
                android:authorities="br.com.ceiot.aula09"
                android:enabled="true"
                android:exported="true">
        </provider>

        <activity android:name=".AddNodeActivity">
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
    <!-- Permissão -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>